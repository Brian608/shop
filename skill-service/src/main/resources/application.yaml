server:
  port: 13000
spring:
  cloud:
    nacos:
      discovery:
        service: skill-service
      server-addr: localhost:8848
  datasource:
    url: jdbc:mysql://localhost:3306/skill
    username: root
    password: root
  redis:
    host: localhost
    port: 6379